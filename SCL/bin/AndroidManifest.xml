<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sistem2.informatica.sclrotas"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />
    <!-- Permissões -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="br.com.scl.view.PrincipalActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="br.com.scl.view.MenuSCLActivity"
            android:label="@string/title_activity_menu_scl" >
        </activity>
        <activity
            android:name="br.com.scl.view.ProdutoresActivity"
            android:label="@string/title_activity_produtores" >
        </activity>
    </application>

</manifest>